#! /bin/bash
echo “hello world”

# Start every run_project.sh with '#!/bin/bash' or '#!/bin/sh' to tell the system that it is a shell script. Otherwise type which into console. 
# After creating shell script, type chmod +x run_project.sh to change permissions for shell script to be executable by others.